import { defineConfig } from 'slidev'

export default defineConfig({
  title: 'My Slide Presentation',
  description: 'A brief description of my presentation',
  theme: 'default',
  layout: 'default',
  css: [
    // Custom CSS can be added here
  ],
  markdown: {
    // Markdown options can be configured here
  },
  // Additional configurations can be added as needed
})